// Faça uma função que imprima na tela apenas os números ímpares entre 1 e 50.

const prompt = require('prompt-sync')()
let p1 = 3
while ( p1 <= 50){
    console.log(`exiba numeros impar: ${p1}`)
   p1 = p1 + 3
}
console.log ('FIM');